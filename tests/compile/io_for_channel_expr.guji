# D4 dogfood regression: native must materialize a channel-valued iterator
# expression instead of requiring an intermediate local binding.
sub main(): Int {
    mut $n = 0
    for $line in stdin.lines() {
        print("line:" ~ $line)
        $n = $n + 1
    }
    print("count=" ~ "$n")
    0
}
