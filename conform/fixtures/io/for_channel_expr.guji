# gujiconform -- D4: channel-valued for iterators may be expressions, not only locals.
sub main(): Int {
    mut $n = 0
    for $line in stdin.lines() {
        print("line:" ~ $line)
        $n = $n + 1
    }
    print("count=" ~ "$n")
    0
}
