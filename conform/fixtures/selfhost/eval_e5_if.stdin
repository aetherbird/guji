# §6 `if` is an expression; this exercises both the then and else branches.
sub label($b: Bool): Str {
    if $b {
        "yes"
    } else {
        "no"
    }
}
sub main(): Int {
    print(label(true))
    print(label(false))
    0
}
