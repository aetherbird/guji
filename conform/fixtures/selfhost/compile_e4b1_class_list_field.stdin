class Bag {
    has @.items: List[Int]
    has $.name: Str
}

sub mk(): Bag {
    Bag(items: [4, 5, 6], name: "bag")
}

sub score($b: Bag): Int {
    print($b.name)
    $b.items.count() + $b.items[1]
}

sub main(): Int {
    $b = mk()
    score($b)
}
