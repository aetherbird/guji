# §4 mutable binding: `mut` allows reassignment, and the final main-frame dump
# must show the updated value.
sub main(): Int {
    mut $total = 0
    $total = $total + 5
    $total = $total + 10
    print($total)
    0
}
