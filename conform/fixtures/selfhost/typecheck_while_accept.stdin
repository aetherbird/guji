# §6.3: a well-typed while loop — Bool condition + mutable index reassignment.
sub main(): Int {
    mut $i = 0
    mut $acc = 0
    while $i < 10 {
        $acc = $acc + $i
        $i = $i + 1
    }
    $acc
}
