# §5.5 operator precedence: the parse tree encodes precedence, and the
# evaluator must walk that tree without reordering it.
sub main(): Int {
    print(2 + 3 * 4)
    print((2 + 3) * 4)
    print(10 - 2 - 3)
    print(2 * 3 + 4 * 5)
    print(0 - 2 * 3)
    0
}
