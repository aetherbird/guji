sub made(): Map[Str, Int] {
    {"b": 2, "a": 1}
}

sub main(): Int {
    %m: Map[Str, Int] = {"z": 4, "a": 1}
    mut $sum = 0
    for $k, $v in %m {
        $sum = $sum + $v
    }
    for $ck, $cv in made() {
        $sum = $sum + $cv
    }
    $sum
}
