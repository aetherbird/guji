# G5b-b: a Str-arg call on the left of a comparison feeding an `if` condition.
sub score($s: Str): Int {
    if $s == "ok" { 7 } else { 3 }
}
sub main(): Int {
    if score("ok") == 7 { 1 } else { 0 }
}
