# P8d-2c-3d-2c: unwrap-on-closed for a scalar-enum channel — exit 1 and the
# fixed "unwrap on None" panic (three-way stderr vs native).
enum Signal {
    Stop
    Go
    Wait($secs: Int)
}

sub main(): Int {
    $ch: Chan[Signal] = channel(1)
    $ch.close()
    $a = $ch.recv().unwrap()
    0
}
