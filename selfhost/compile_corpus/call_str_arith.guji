# G5b-b: a Str-arg call as an operand of Int arithmetic needs argument prep,
# so the `+` operand can no longer be a pure rvalue.
sub score($s: Str): Int {
    if $s == "ok" { 7 } else { 3 }
}
sub main(): Int {
    score("ok") + 1
}
