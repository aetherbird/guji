# I10a2: §17.2 negative channel capacity. C's guji_chan_new panics with a fixed
# message (no "got N"); D must match that exact stderr and exit 1.
sub main(): Int {
    print("before")
    $ch: Chan[Int] = channel(0 - 1)
    print("unreachable")
    0
}
