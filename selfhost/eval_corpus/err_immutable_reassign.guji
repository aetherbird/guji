# §4 reassigning an immutable binding in the same frame is rejected before
# execution (no `mut`). This is not nested-scope shadowing.
sub bump($n: Int): Int {
    $n = $n + 1
    $n
}
sub main(): Int {
    print(1)
    print(bump(5))
    0
}
